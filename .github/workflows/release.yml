name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  CMAKE_C_FLAGS: "-march=armv8.2-a+crypto+simd"
  CMAKE_CXX_FLAGS: "-march=armv8.2-a+crypto+simd"

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install protoc
        run: brew install protobuf
      - name: Build release binaries
        run: cargo build --release
      - name: Strip binaries
        run: |
          strip target/release/vcm
          strip target/release/vcmctl
          strip target/release/vcmd
      - name: Package
        run: |
          VERSION=${GITHUB_REF_NAME}
          tar czf vcm-${VERSION}-aarch64-apple-darwin.tar.gz \
            -C target/release vcm vcmctl vcmd
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: vcm-*.tar.gz
          generate_release_notes: true
